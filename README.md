# TOP Compass

Singapore's TOP/CSC building-completion process, explained — for home buyers (root pages) and
industry professionals (`/pro/`, including a dedicated data-centre section).

Pure static HTML/CSS in `site/`, deployed to GitHub Pages by
`.github/workflows/deploy-pages.yml` on every push to `main`.
